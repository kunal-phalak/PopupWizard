import SwiftUI

/// Defines constants used in the popup system.
public enum Constants {
    /// Duration for animations.
    public static let ANIMATION_DURATION: Double = 0.28
    /// Default animation style.
    public static let ANIMATION: Animation = .snappy(duration: ANIMATION_DURATION)
}
